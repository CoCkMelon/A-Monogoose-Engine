AGENT_HANDOFF — ame-next
Last updated: end of session 1 (bootstrap + Stage 0 local slice)

STATUS: engine core + first game (LOCAL Memory, Stage 0) build clean on
Linux/clang-19 C23, 9 ctest suites green incl. headless GOLDEN RENDER
(EGL surfaceless + llvmpipe) and a headless game SCREENSHOT smoke.

WHAT EXISTS (all tested)
- include/ame/: ame.h (handles, rng, snapshot macro), math.h (v2/v3/v4, m4,
  ortho-pixel + perspective, inverse), pool.h (slot template: gen handles,
  DEFERRED despawn), geometry.h, events.h, input.h, audio.h, camera.h,
  text.h, render.h, app.h
- src/: geometry.c (prims + static world + uniform-grid broadphase, 2D/3D),
  events.c (bounded ring, drop-oldest), input.c (atomics + per-step edges),
  audio.c (fixed-point-phase synth, SPSC cmd queue, SDL stream attach),
  camera.c (fluent setup builder; persp + pixel-perfect ortho), text.c
  (utf-8, {c=}{p=} tags, wrap/align, screen+world draw), render.c (one
  program, counting-sorted batch, texture-grouped draw ranges, injected GL
  loader, FBO-compatible), app_sdl.c (SDL3 callback bootstrap, logic thread
  1000 Hz, in_begin_step + app_fixed, SDL input backend), input_asyncinput.c
  (guarded TU: AME_INPUT_ASYNCINPUT; evdev->scancode map, atomics only)
- examples/memory_game/: mem_sim (pure deterministic rules engine; tests
  play full scripted games) + mem_app (3D board, flip animation, raycast
  picking via geometry module, in-scene billboard scoreboard, synth sfx,
  snapshot publish logic->render, AME_SCREENSHOT env for headless shots)
- tools/bake_font.c: ttf -> generated/font_atlas.c/h at build time
- tests/: math, pool, geometry_2d/3d, input, audio, text, mem_sim, render
- .github/workflows/ci.yml: linux (both dims, headless tests, screenshot
  smoke, release build) + ASan job
- docs/: the full spec (copied verbatim from the engine_spec upload)

SESSION 2 FIXES (user-reported, all verified headless)
- Software cursor: system cursor hidden (SDL_HideCursor), game draws an
  in-scene camera-facing quad where the mouse ray meets the table
  (draw_cursor, render thread reads input atomics directly - sub-tick).
- Hover: logic thread raycasts mouse->card each 1 ms step, eases per-card
  lift (g_lift, ~50 ms) + highlight, published in the snapshot. Pickable
  only in PICK1/PICK2 on face-down unmatched cards.
- Resize stretch: app_resize(w,h) hook (app.h); app_sdl rebuilds nothing
  itself - game rebuilds camera (viewport+build+rp_set_camera).
- Text orientation: billboard_pose had layout-down -> world-UP (vertical
  mirror); now col1 = -u. Card labels: face normal was (0,ca,-sa) i.e.
  INTO the table when open; correct panel basis is R_x(-a):
  v=(0,sa,ca), n=(0,-ca,sa). Ground-truth test renders glyph 'F' and
  correlates pixels vs the atlas bitmap (direct must beat hflip ~4x+).
- text_draw_screen hardcoded glyph scale 1 ignoring the layout scale -
  layout now carries its scale and draw honors it (test caught it).

SESSION 3 (resize + orientation hardening)
- Engine now syncs the game to the window's REAL pixel size at startup
  (SDL_GetWindowSizeInPixels after app_init; app_resize hook) - WM may open
  windows at non-default sizes/scales; resize events also re-query pixels
  because RESIZED can carry POINTS under Wayland/mac scaling.
- AME_WINDOW_W/AME_WINDOW_H env override the initial window (testing/CI).
- rp_screen_origin(): the 2D ortho camera centers world (0,0) at screen
  CENTER (pos = view center). Screen-anchored UI text adds the origin.
- Orientation now PROVEN on all three text paths (screen / world identity /
  billboard pattern) by atlas-correlation SSD (direct ~1.4-21 vs flipped
  ~62-79). Sampler needs the EXACT cell origin (line origin + xoff/ascent
  offsets) and PER-GLLYPH-PIXEL step, not bbox inference.
- NDC->px mapping: px = (ndc*0.5+0.5)*vw, y flipped; forgetting the +center
  offset breaks edge/position assertions silently (ratios still pass).

PITFALLS HIT THIS SESSION (do not rediscover)
- SDL_main.h: Debian SDL3's SDL.h does NOT pull SDL_main.h; the bootstrap
  defines SDL_MAIN_USE_CALLBACKS + includes <SDL3/SDL_main.h> explicitly in
  ONE TU. Tests define SDL_MAIN_HANDLED (they own main()).
- GL function typedefs: use <GL/glcorearb.h>; Khronos PFN names are ALL
  CAPS: PFNGLCREATESHADERPROC (not PfnglCreateShaderProc).
- EGL surfaceless has NO default framebuffer: tests must create an FBO and
  bind it before rp_begin_frame; read pixels from the FBO.
- Baked-atlas header: never name a size macro like the include guard
  (AME_FONT_ATLAS_H height collided with the guard -> now ..._WIDTH/_HEIGHT).
- text_draw_world maps glyph PIXELS to world units directly: scale the pose
  (see card_label_pose gs / billboard_pose scale in mem_app.c).
- Generated font_atlas.c: glyph table must be emitted closed BEFORE the
  atlas byte array opens (array nesting).
- CMake: tools/ (bake_font) must come BEFORE the engine libs (they link
  ame_font). asyncinput's own "smoke" ctest leaks: BUILD_TESTING is toggled
  OFF around its add_subdirectory.
- pass camera CENTER in 2D: world origin = screen center with pos(0,0).

NOT DONE / NEXT ACTIONS (in order)
1. Windows job: MinGW-w64 or clang-cl cross build in CI (engine is
   POSIX-clean; needs SDL3 for the target + the same C23 pin).
2. Web job: Emscripten build of the SAME C (rp GL loader ->
   emscripten_proc_address; GLES desc path exists: rp_desc_gles(true);
   app shim maps the callbacks; text/atlas already data-only).
3. Android job: NDK toolchain + SDL3 android bootstrap (CMake), GLES.
4. Stage 1: ONLINE server-authoritative Memory (README.txt FIRST GAME):
   run mem_sim on a server; message set join/state/your-turn/open-card/
   match/no-match/win; headless two-client loopback test in CI.
5. libfyaml vendoring + dialogue/save/settings modules (specs in docs/;
   NOT part of the first game slice).
6. tools/assimp importer -> cooked model loader (levels.txt).
7. audio occlusion raytracer port from A-Mongoose (audio.txt, later stage).
8. Spec/code drift CI check (agents.txt): header changed -> docs/ changed.

CONVENTIONS LOCKED
- C23 everywhere in engine+game; third_party keeps its own standard, no
  -Werror there. -ffp-contract=off always. Per-target AME_2D/AME_3D and
  AME_INPUT_SDL/AME_INPUT_ASYNCINPUT defines. Games live in examples/ with
  their own CMakeLists (user decision, like A-Mongoose).

SESSION 4 PITFALLS + STATE (picking fix + Lean model)
- ROOT CAUSE of hover misalignment: ame_m4_inverse was mathematically wrong
  (hand-transcribed cofactors). ONLY picking consumed it -> visuals masked it
  for weeks. Fix: gl-matrix adjugate rewrite + M*inv(M)=I test in
  test_camera.c. LESSON: never hand-transcribe matrix algebra without an
  inverse round-trip test. camera_screen_ray is now ANALYTIC (camera basis,
  no inverse at all); board picking round-trip misses 0.0000 on all 16 spots.
- Cursor is a shaded 8-face CONE (rp_push_tri -> degenerate quad). Hot state:
  bigger + amber. AME_FAKE_MOUSE=x,y env drives headless hover proofs.
- Lean model (lean/): elan lives in ~/.elan (survives snapshots usually;
  if gone: curl elan-init.sh, `elan default stable`, ~13s).
  Lean 4.33.1 pitfalls hit:
  * structure fields CANNOT share a binder (refA refB : Nat -> "Failed to
    infer type of binder") - one field per line.
  * List.get? gone; List.getElem? NOT reachable via dot notation; use
    l[i]? index syntax; full `simp at h` reduces (c::cs)[k+1]? fine.
  * List.length_drop is an equality, not a function - rw it, don't apply.
  * `rw [h] at h'` fails hard if pattern absent in h' - rw goal only.
  * theorem with section `variable (step)` + local step binder: OK, variable
    just isn't included.
  * no deriving Repr on structures with function fields; no doc-comment
    directly before `namespace` (use /- -/ with space); @[default_target]
    annotation on lean_lib REQUIRED or lake builds nothing.
- Lean acceptance met: lake build green from clean, zero sorry, no mathlib,
  #print axioms on all 14 theorems = stdlib-only (3 axiom-free).
- lean/.lake is gitignored (added).
- Stage 1 online Memory: still NOT started (user has not greenlit).

SESSION 4 ADDENDUM - LEAN GEOMETRY MODEL (user directive: "primary use
case for lean here is geometry, pixel coords, other coord conversions,
so shaders could also be verified"):
- New modules: Ame/Geo (vectors, px<->NDC round-trips both axes/both
  directions), Ame/M4 (mat4 layer + adjugate inverse transcribed from
  ame_m4_inverse + structured-inverse theorems + 4 native_decide
  concrete M*inv(M)=I checks mirroring test_camera.c), Ame/Camera
  (rayRaw = raw camera_screen_ray dir; ray_hits: pos + D*ray = P),
  Ame/Shader (vertShader = gl_Position line, gpuViewport = divide +
  viewport; shader_pixel_x/y == analytic pixel; picking_from_shader =
  end-to-end GPU-pixel -> analytic-ray -> exact hit). 27 kernel-checked
  theorems + 4 quarantined native_decide checks.
- LEAN-RAT PITFALLS (cost the most time):
  * Kernel CANNOT reduce Rat ops (extern): `decide` is DEAD for all Rat
    facts, even 1+1=2. Workarounds: numeral adds/muls via
    `simp [Rat.add_def, Rat.normalize_eq]`; DIVISION is not
    simp-evaluable at all (divInt blocks) -> concrete division checks
    must use native_decide (disclosed) or be avoided.
  * Structure fields cannot share binders (refA refB : Nat -> error).
  * `+`/`*` on own structures go through instances that simp CANNOT
    unfold: write defs used by proofs with explicit V3.add/V4.add
    nesting, and state hypotheses with explicit V3.add.
  * `show` requires DEFEQ, and Rat add is NOT assoc-defeq (extern):
    parenthesization of sums in `show` patterns must match exactly.
  * rw direction discipline: rw [h] replaces h's LHS; rw [<- h] its
    RHS; `rw ... at h'` fails hard if the pattern is absent (no
    partial application) - rw the goal, carry hypotheses manually.
  * Full-AC simp (mul_comm+assoc+left_comm+add_*) can TIMEOUT on 4x4
    matrix products: `set_option maxHeartbeats 4000000 in` fixed
    mulv_mul (60s build). Keep simp sets minimal per goal.
  * Long chained projections collect paren bugs; use local defs
    (vp3) + `simp only [vp3]` to unfold.
- Cone cursor + AME_FAKE_MOUSE hover proof unchanged (54ccb5b).
- Stage 1 online Memory: NOT started (network is stage-1 implementation
  work, NOT a Lean target - per user).

SESSION 5 - STAGE 0 CLOSED (packaged unit + clean-machine smoke):
- Gap audit vs the spec's Stage 0 EXIT line found two open items: seed
  replay ergonomics and "packaged unit runs on a clean machine"
  (build.txt PACKAGING + deploy.txt). Both closed; everything else was
  already green.
- AME_SEED env in mem_app (default 0xC0FFEE unchanged -> golden hashes
  stable); seed printed at boot. Replay proofs: test_mem_sim trace-hash
  (existing) + packaged smoke byte-identical screenshots (AME_SEED
  0x5EED twice -> cmp equal).
- deploy.txt packaging: root CMake links SDL3 STATICALLY in Release
  when the SDL3::SDL3-static target exists (Debian ships it; its
  interface still pulls shared sndfile/pulse/X11 deps - those get
  bundled); memory_game gets $ORIGIN/lib rpath in Release ONLY (plain
  user build unchanged, shared SDL3). tools/pack.sh: release build ->
  bundle FULL dep closure (ldd of binary AND every bundled lib to
  fixpoint; PITFALLS: glibc ldd prints per-file "path:" HEADER lines
  when given multiple files - filter /:$/ or cp chokes on a trailing
  colon; libpulsecommon lives in a private pulseaudio/ dir found via
  the ORIGINAL file's runpath - resolve at original location) -> run.sh
  (LD_LIBRARY_PATH=lib) + README.txt -> dist/ame-next-stage0-linux-
  amd64.tar.gz + sha256. tools/smoke.sh: unpack to empty tmp dir, dep
  audit (bundle-or-base ONLY, run with the launcher's LD_LIBRARY_PATH),
  headless PNG magic+size check, seed-replay cmp. Verified BOTH paths
  here: static unit (40 libs) and dynamic fallback (-DAME_SDL3_STATIC
  OFF, libSDL3.so.0 bundled). CI: package + clean-smoke jobs appended
  (clean-smoke installs RUNTIME packages only, no -dev).
- Slimming the bundle (custom SDL_LEAN_AND_MEAN build per deploy.txt)
  is a LATER-stage nicety; bundling is the spec-sanctioned fallback.
- Stage 0 EXIT checklist now fully green: hot-seat 2-player with 3D
  flips + billboard scoreboard, strict alternation (turn passes every
  resolve), most-matches-wins + tie, fixed-seed replay deterministic,
  headless screenshot, clean -Werror build + 10/10 ctest, packaged
  unit passes clean-machine smoke.

SESSION 6 - STAGE 1 ONLINE (server-authoritative Memory) IMPLEMENTED:
- examples/memory_game/mem_net.h/.c: TCP framing [type a b c len16 LE]
  + payload; message set exactly the spec's join/state/your-turn/
  open-card/match/no-match/win (+left/bye/welcome/opened/quit); POSIX
  socket helpers (nonblocking data, streaming rx reassembly); client
  MIRROR driver (mem_client): OPENED -> mem_pick on the local mem_game
  (identical deterministic flip animation), STATE -> full overwrite
  (join/rejoin/rematch). v0 NOTE: STATE carries face-down pairs (a
  memory-scanning client could peek) - hiding behind reveal events is
  later hardening; exit criteria do not require secrecy.
- mem_server.c: authoritative; owns the ONE mem_game; accepts 2 slots;
  validates OPEN via the sim itself (turn+phase+card) and broadcasts
  the echo. Turn of an ABSENT player = natural pause (no turn theft);
  rejoin = full STATE snapshot + resume; rematch = JOIN votes after
  WIN; clean exit SIGINT/SIGTERM (BYE) or MEM_IDLE_EXIT idle timeout.
  MEM_TIME_SCALE env speeds the authoritative sim (safe: server only
  ever WAITS for discrete intents).
- mem_app.c: AME_SERVER=host:port online mode - thin view: local sim
  becomes the render-only mirror, clicks send OPEN intents, scoreboard
  shows YOU labels + YOUR/opponent turn + YOU WIN/LOSE + opponent-left
  + server-closed status; rematch click = JOIN vote; unreachable
  server falls back to LOCAL hot-seat (never dead-ends). Local mode
  byte-identical (deterministic screenshots verified). New envs:
  AME_SERVER, AME_SCREENSHOT_FRAMES.
- tests/test_mem_net.c (ctest: mem_net, ~15s): spawns the REAL
  mem_server binary (free loopback port), two scripted clients play a
  full game to WIN (winner+scores+alternation verified on BOTH
  mirrors), rematch, drop-mid-game (LEFT, no turn theft within 400ms,
  no flip), rejoin (mid-game STATE equality), resume to completion,
  SIGTERM -> BYE -> exit 0.
- BUGS FOUND + FIXED (all logged): (1) any_client computed at loop top
  but idle check at bottom -> idle-exited on the very iteration that
  ACCEPTED the first client (read slot occupancy LIVE); (2) same class:
  empty_since==0 not-yet-rearmed treated as epoch-0 -> instant idle
  exit right after the LAST client dropped (guard empty_since != 0);
  (3) accept-then-JOIN-message double-join duplicated game-start
  STATE+TURN -> looked like broken alternation (accept only reserves
  the slot; JOIN message drives the join state machine; rematch votes
  separate); (4) uninitialized slot_ix[] read for just-accepted fd
  (stack garbage POLLHUP -> instant drop); (5) test mirrors were never
  mem_step'd (frozen in REVEAL1); (6) ninja hides compile errors when
  piped to grep -c (grep exits 1 on zero matches -> && chain breaks or
  stale binary runs SILENTLY - always check build freshness, e.g.
  strings binary | grep new-symbol).
- CI: mem_net is a normal ctest (self-contained loopback); no CI
  changes needed. mem_server builds with the default target (pure
  POSIX + sim; AME_3D define satisfies ame.h; _POSIX_C_SOURCE
  200809L needed under -std=c2x for sigaction/clock_gettime).

SESSION 6 ADDENDUM - ANTI-PEEP PROTOCOL + POLISH:
- mem_net: STATE now withholds pairs of cards whose state is DOWN
  (MEM_PAIR_HIDDEN 0xFF); open/matched/closing cards are public (a flip
  showed them - exactly what an honest player knows). OPENED carries
  the true pair in field c; the mirror sets it before its animation
  reaches RESOLVE (TCP ordering + flip duration guarantee it), so the
  deterministic mirror still computes match/no-match exactly. SDL
  client needed NO change (label draw already bounds-checked pair).
- test_mem_net: policy is now HONEST-MEMORY (uses only revealed pairs)
  - doubles as the compliance proof (game completes with hidden pairs
  end-to-end); mirrors step at 4x (cosmetic pacing) -> suite ~7s;
  asserts 16/16 pairs hidden on a fresh board and hidden pairs in a
  rejoin snapshot.
- mem_server seed now mixes time ^ MONOTONIC nanos ^ games++<<13
  (time(NULL)^const repeated boards within one second - spotted via
  two identical 3:5 games in the test log).
- Repo root README.md added (build/test/run local+online/headless/
  env vars/layout/status table).

SESSION 7 - STAGE 2 SLICE 1: FORWARD LIGHTING (the 3D path stops
being unlit; render.txt still SINGLE PASS - the multipass decision
stays deferred until a real need, e.g. shadows):
- render.h/.c: ONE shader gains u_ldir/u_lcol/u_lamb + point light
  (u_ppos/u_pcol/u_prange, range<=0 off). Per-VERTEX lit flag (new
  `lit` float channel, attrib 4) so world geometry and unlit UI share
  the single batch: rp_set_lit(1)/rp_set_normal stamp subsequent
  pushes (tint-style), text/billboards stay unlit and BYTE-IDENTICAL
  (proven: unlit frame hash equal before/after setting lights).
  Defaults reproduce v0 exactly. Loader needed UNIFORM3FV/UNIFORM1F
  X-entries. mix(vec3(1),light,a_lit) keeps ES3/desktop shared GLSL.
- Game: warm key light + cool ambient + soft point light over the
  table; cards lit via the VISIBLE face normal (flip catches the
  light; |cos| trick selects the camera-facing side); table lit;
  cone cursor now engine-lit amber/bronze (manual shading removed).
- test_render: lighting case - unlit frame identical after setting
  lights; lit quad facing vs away differs by the card's full-tint
  delta (absolute margin > 50000: the card is only ~330px on screen;
  a percentage-of-frame assert was wrong).
- Lean: Ame.Shader.max0/shadeTerm/shade_backface (backface == ambient
  exactly; standard axioms only). Rat order lemmas: neg_le_neg +
  neg_zero (no zero_lt_one in core - skipped the fully-facing
  variant).
- Stage 2 next: materials/particles, shadow pass (the actual
  multipass decision), assimp mesh loading.

SESSION 7 ADDENDUM - STAGE 2 SLICE 2: THE MULTIPASS DECISION + POST:
- Made the deferred multipass decision (render.txt): ONE geometry
  program/batch unchanged; rp_desc_post(on) renders each frame into an
  offscreen RGBA8+D24 scene target, then a tiny fullscreen-triangle
  program (no depth/blend) composes it back with tint*vignette.
  Identity settings are PIXEL-EXACT with the direct path (ctest).
- HARD-WON LESSONS (all bit me here):
  * rp_end_frame EARLY-RETURNED on an empty batch - with post on that
    left the scene FBO bound; the next begin_frame then captured the
    SCENE TARGET as its compose target (frame fed back into itself;
    everything after rendered into a deleted FB). Compose now ALWAYS
    runs when post is on.
  * The compose target is NOT "framebuffer 0": under the surfaceless
    EGL test harness (and embedded hosts) the app draws into a
    user FBO. begin_frame now records GL_FRAMEBUFFER_BINDING and the
    compose returns to THAT.
  * scene_target_ensure must RESTORE the previous binding (it bound 0
    at the end of setup, so the first begin_frame captured 0).
  * The compose shader originally forced alpha=1; glyph-edge alpha
    from blending differs => NOT pixel-exact. Pass scene alpha
    through (s.a).
  * rp_shutdown+rp_init orphans the font atlas id: text_init(true)
    must re-register after every re-init in tests.
  * The AME_GL_FUNCS X-macro: an inserted entry line with no trailing
    backslash TERMINATES the macro (hit THREE times; loader entries
    must end in " \\").
- test_render post cases: identity == direct hash, resize cycle
  (halve/restore) keeps resources correct, vignette corners darken
  (center 193 vs corner 56 lum). Game: rp_desc_post + vignette 0.28
  (deterministic across runs); shots/stage2_post_vignette.png.
- Loader additions: FBO/renderbuffer fns, DRAWARRAYS, GETINTEGERV,
  UNIFORM3FV/UNIFORM1F. rp_viewport grows/shrinks the scene target.

SESSION 7 ADDENDUM 2 - STAGE 2 SLICE 3: PARTICLES (single pass only):
- include/ame/particles.h + src/particles.c: SoA pool (2048), pt_spawn/
  pt_step (gravity+drag, swap-with-last removal)/pt_draw (camera-facing
  billboards from the camera basis, size0->size1 + col0->col1 fade,
  unlit, ONE rp batch). NO RNG in the module - callers parameterize
  spawns, so effects are reproducible.
- GAME EFFECTS ARE A PURE FUNCTION OF THE SNAPSHOT: the first
  (stateful, display-side) implementation broke screenshot determinism
  - accumulated particle state depends on WHICH sim t each display
  frame samples. Rebuild-from-scratch each frame instead: sim stamps
  card.matched_at / game.over_t (mem_sim), the app recomputes
  closed-form trajectories (linear damping has a closed form:
  p(a) = o + v0(a - k a^2/2) + g a^2/2) into a scratch pool and draws.
  Frame = f(snapshot) only -> byte-deterministic MID-ANIMATION captures.
- AME_FIXED_FRAME_DT (app_sdl.c): QA mode - no logic thread; iterate
  calls app_fixed(dt) inline per rendered frame, so frame N sees
  exactly sim t = N*dt. With AME_AUTOPLAY=1 (local-only honest-memory
  bot) captures are deterministic at ANY frame index (proven: frames
  200..950 byte-identical across runs, bursts + confetti in flight).
- REAL BUG FIXED by this slice: app_fixed hardcoded mem_step(SIM,
  0.001f) - correct only for the 1 kHz thread; now steps by its dt
  parameter (thread path identical, dt=0.001).
- Tests: test_particles (pool semantics, no GL: dense front, visible
  overflow drop, swap-remove identity via velocity signatures,
  bit-identical lockstep states) + render case (64 billboards = 64
  quads, stable hash, expiry to zero). 12/12 ctest. Quiet golden
  UNCHANGED (no input -> no effects -> byte-identical frame 5).
- shots/stage2_particles.png: autoplay frame 500 (matched cards green,
  bursts flying) - byte-reproducible via AME_FIXED_FRAME_DT=0.0166667
  AME_AUTOPLAY=1 AME_SCREENSHOT_FRAMES=500.

SESSION 8 - A-MONGOOSE PARITY (user: "reach feature parity with
original a mongoose"). Inventory from github.com/CoCkMelon/
A-Monogoose-Engine. REJECTED per spec (not parity targets): ECS/flecs,
Unity-like facade, scene YAML/prefabs, parent-child transform
hierarchy, Box2D bridge (opt-in per project later).
PARITY ITEMS LANDED THIS SESSION:
- audio_ray.h/.c: 2D occlusion raytracer PORTED per audio.txt
  (distance rolloff + air absorption dB/m + per-shape materials
  transmission_loss_db/mono_collapse + occlusion_db fallback +
  constant-power pan; presets AIR/STEEL/WOOD/CONCRETE/DRYWALL).
  Key change vs A-Mongoose: occlusion counts each shape ONCE via
  per-shape segment tests (nearest-hit marching re-counts a thin wall
  entry+exit+inside); materials live in a fixed table keyed by shape
  id - ame_audio_ray_reset() MUST pair with ame_geo_reset (stale ids
  keep materials otherwise). ame_audio_spatial_gains = cheap no-ray
  path (audio.txt "plain spatial helpers").
- audio: AME_WAVE_PCM decoded voices (stereo interleaved float buffer
  owned by caller, loop or one-shot) + audio_load_wav (16-bit PCM
  mono/stereo RIFF; offsets: 20=fmt tag,22=channels,24=rate,34=bits
  - do NOT conflate fmt/channels). audio.txt DECODED scope, no
  streaming.
- tilemap.h/.c: Tiled .tmj (JSON) parity port (orthogonal, right-down,
  int layers, single tileset, FIXED pools no malloc): minimal JSON
  subset parser + ame_tilemap_draw pushing one quad per non-zero gid
  into the ONE rp batch (atlas UVs when a tileset atlas is passed,
  stable gid tints on white otherwise).
- test_render_2d (ame::2d + AME_2D define!): CLOSES the render.txt
  CHECK line "2D build renders 1000 sprites in a handful of draw
  calls with a pixel-perfect camera" - 1036 quads in 1 draw call;
  +0.9px camera move = byte-identical frame (snap), +1.0px changes;
  viewport cycle stable. PITFALLS: test TUs linking ame::2d MUST
  -DAME_2D=1 (struct layout ABI differs between dims!); remember
  rp_set_gl_loader BEFORE rp_init (-2).
REMAINING PARITY: dialogue (libfyaml runtime + bake-to-C +
determinism test, dialogue.txt), mesh import (Assimp build-time tool
-> C + rp_push_mesh per-object world matrix, levels.txt), opus decode
(Stage 3 codecs).

SESSION 8 ADDENDUM - PARITY 2/3 AND 3/3 (dialogue + mesh import):
- tools/assimp2c.c (optional CMake target when assimp found): bakes
  models to C arrays at BUILD time (levels.txt); engine rp_push_mesh
  (amesh_vert pos/nrm/uv, optional column-major xform, per-vertex
  normals under rp_set_lit, per-tri UV normalize); baked cube
  COMMITTED at tests/assets/baked_cube.h so tests need no assimp.
  %.6ff in the emitter - "%gf" prints "0f" (invalid C).
- dialogue (libfyaml, apt libfyaml-dev; CMake drops dialogue.c when
  absent - engine/tests build fine): the TIGHT format of
  dialogue.txt - aliases/speakers map, default speaker (RESOLVED
  through the table: "default speaker: G" -> "Glitcher"), bare quoted
  strings (current speaker), flow/block maps, per-line portrait/on
  (string or list), choices [{button: label}] with empty target =
  end. DISAMBIGUATION IS BY KEY, not value (the spec's "value"
  wording is misleading: "{V: ...}" switches because V is a known
  alias; "{mid: ...}" labels because mid is not; "do not name a
  label the same as a speaker" confirms). Walker = A-Monogoose
  semantics: advance BLOCKS at a choice point, select jumps to the
  label, on-events fire once per line (take_events).
  PITFALLS: fy_node_get_scalar is NOT zero-terminated - always copy
  via len (node_scpy/key_is); libfyaml 0.8 names:
  fy_node_mapping_item_count / _get_by_index /
  fy_node_mapping_lookup_value_by_string(n,k,len). DETERMINISM RULE:
  runtime vs baked (tools/dlg2c.c) scenes must be memcmp-IDENTICAL -
  a speaker written twice ("Glitcher" then "Venera") leaves stale
  bytes past the NUL: memset target buffers before overwrite. Test
  asserts file parse == string parse == baked.
- CI: libfyaml-dev + libassimp-dev added to the apt lines.
- Parity matrix vs A-Monogoose-Engine: KEPT+DONE: 1kHz loop, split
  threads, deterministic audio, builders, text atlas, camera,
  asyncinput, tilemap, dialogue, mesh import, audio raytracer,
  decoded samples, pan. REJECTED per spec (never parity targets):
  flecs ECS, Unity-like facade, scene YAML, prefabs, parent-child
  hierarchy, Box2D-as-core. ame-next is AHEAD: 3D, lighting, post,
  particles, online server-authoritative game, Lean model, packaging.
  Remaining: opus decode (Stage 3 codecs).

SESSION 9 - AUDIT + GEOMETRY LIB IMPROVEMENTS:
AUDIT (full suite under ASan+UBSan = clean; 7 bugs found by probes +
review, ALL fixed + regression-tested):
- ame_geo_ray_sphere assumed |d|=1 (t wrong for any other length,
  contradicting the header's own "|d| scales tmax" contract) -> full
  quadratic in a=d.d; t now in |d| units like ray_aabb.
- ame_geo_ray_aabb from inside -> ZERO normal (garbage for lighting/
  reflection) -> deterministic embedded normal (dominant travel axis,
  opposing direction).
- ame_m4_look_at with f parallel to up (straight down/up) -> zero
  right axis -> degenerate view matrix, NOTHING rendered (found via
  the particle test; the billboard guard alone was not enough).
  Guard substitutes a fallback up; non-degenerate cases bit-identical
  (golden unchanged). Particles' camera basis got the same guard.
- audio_load_wav trusted a canonical 44-byte header -> any file with
  a LIST/INFO chunk parsed garbage (frames=1 of 4) -> proper RIFF
  chunk walk (word-aligned skips, fmt size >= 16).
- audio_ray occluded SPHERES by their bounding box (over-charging
  loss near corners) -> candidates from the broadphase (overlap_world
  on the segment AABB) + exact ame_geo_ray_shape per shape.
- rp_push_mesh read OOB on malformed baked indices -> vert_count
  parameter + skip bad triangles (API change; test updated).
- raycast/overlap_world: rays/boxes fully outside the world bounds
  walked clamped edge cells (exact tests rejected them; pure waste)
  -> AABB-vs-world-bounds early-out.
GEOMETRY ADDITIONS (all spec physics.txt "INCLUDED" items now exist):
- distance: point_aabb_dist2 (0 inside), point_sphere_dist (signed).
- capsule (spec shape): overlap sphere/aabb (golden-section over the
  convex segment-box distance), point dist2.
- OBB (spec shape): obb_from_aabb, point_in_obb, obb_overlap SAT
  (2D 4 axes; 3D 15 incl. cross products, parallel axes skipped).
- ame_geo_add_mesh_proxies: mesh -> per-cell AABB proxies ("mesh
  approximated by primitives"; also feeds the future RT broadphase).
- ame_geo_ray_shape(i): exact dispatch per shape kind.
Tests: test_geometry +8 cases in BOTH dims (62+58 checks), audio_ray
+2 (sphere exactness 0-loss near-miss / 18dB dead-center; LIST wav),
render +1 (straight-down camera particles). rp_push_mesh signature
now (tex, verts, vert_count, idx, idx_count, xform, tint, layer).

SESSION 10 - CGLM PARITY (user directive #8: "reach cglm feature and
performance parity or use cglm"). DECISION upheld: parity path, NOT
adoption - cglm stays a TEST-ONLY oracle/benchmark exactly like the
golden screenshots; the engine keeps its zero-dependency, Lean-verified
math layer. libcglm-dev 0.9.6 (apt) + pkg-config gates the test.

API PARITY (include/ame/math.h, all static inline, no alloc):
- ame_v4 arithmetic (add/sub/scale/dot/lerp); full ame_m3 set
  (identity/mul/mulv/transpose/from_m4/inverse via adjugate).
- ame_quat (x,y,z,w) full set: identity/dot/norm/conj/mul (Hamilton)/
  axis_angle/rotate_v3/mat4/from_m4/slerp (shortest path) + nlerp
  near-parallel.
- ame_m4_transpose, ame_m4_axis_angle (Rodrigues via quat).
- ame_frustum: Gribb-Hartmann plane extraction from vp (order
  [L,R,B,T,N,F], inside-positive, normalized) + aabb p/n-vertex and
  sphere classify (OUTSIDE/INTERSECT/INSIDE).
- ame_bezier3, ame_catmull_rom; ame_m4_mul_to pointer form for hot
  loops. -ffp-contract=off determinism unchanged.

SSE2 FAST PATHS (x86-64; scalar fallback compiled elsewhere):
- ame_m4_mul + ame_m4_mul_to: column broadcasts, per-element product
  order preserved -> BIT-IDENTICAL to ame_m4_mul_scalar.
- ame_quat_mul: 4 term-vectors with sign XORs, uniform
  ((t1+/-t2)+/-t3)+/-t4 -> BIT-IDENTICAL to scalar body.
- test_math_cglm asserts memcmp equality vs the scalar twin on EVERY
  oracle iteration (the assertion caught two wrong lane shuffles in
  development - it earns its keep).

PERF (test perf case, serial-dependency anti-DCE both sides,
informational printf; ns/op, same machine, GCC -O2):
    mat4_mul ptr : ours 5.1   cglm 5.4   (0.93x - faster)
    mat4_mul val : ours 27.3  (by-value struct traffic, API choice)
    mulv point   : ours 3.8   cglm 3.6   (1.05x)
    quat_mul     : ours 4.3   cglm 5.3   (0.82x - faster)
    mat4_inv     : ours 47.5  cglm 19.0  (2.5x - ACCEPTED cold path,
                     2 calls per camera update; SSE cofactors would
                     diverge bitwise from the scalar adjugate for no
                     real gain. Documented, not hidden.)

CORRECTNESS: tests/test_math_cglm.c = 2625 checks green:
64 iters m4 (mul/point-transform/inverse/look_at/perspective vs cglm
4e-5..1e-4 eps + bit-identity asserts), 64 iters quat (axis-angle/mul
with +-sign fix for double cover/rotate_v3/slerp/mat4/from_m4 +
bit-identity), 32 iters frustum (plane extraction sign-fixed vs
glm_frustum_planes + aabb/sphere cull agreement vs glm_aabb_frustum).
Deterministic LCG rng 0x12345678.

VERIFY (all green this session): ctest 16/16 normal, 16/16 ASan/UBSan,
fresh default-config build -Werror-clean (0 warnings), quiet golden
(frame 5) + autoplay-500 golden (stage2_particles.png) BYTE-IDENTICAL
with SSE live - proves bit-identity through the whole render pipeline.

NOTES for next session:
- cmake re-run needed once: tests/CMakeLists.txt gained a conditional
  pkg_check_modules(CGLM QUIET cglm) -> math_cglm test only when cglm
  is installed (status message otherwise, suite is 15 without it).
- cglm C API details that cost time: mat4 is vec4[4] (flat index via
  [i/4][i%4]); glm_quat_mul (not mulq); glm_aabb_frustum returns bool;
  _mm_set_epi32 order is (e3,e2,e1,e0); _MM_SHUFFLE(d3,d2,d1,d0).
- Remaining parity: opus decode. Stage 2 exit: shadows + materials.

SESSION 11 - EXTERNAL-REVIEW FIXES (branch agent/ame-next-20260905-4ad0fd06).
An external comparison (A vs B-rewrite vs master) verified our 16/16
but found integration + concurrency defects. All fixed + tested:

1. SUBMODULE METADATA: third_party/asyncinput was a gitlink with NO
   .gitmodules -> fresh checkout unbuildable. .gitmodules added (repo:
   github.com/CoCkMelon/asyncinput, recorded commit 1f3d0cd == its
   main HEAD). CMake now fails LOUDLY with the fix command if the
   submodule dir is empty. README says --recurse-submodules.
2. SNAPSHOT DATA RACE (their TSan repro): the old double-buffer
   "latest pointer" could be overwritten mid-read (1000 Hz publisher
   vs frame-long reader holds). AME_SNAP_DEFINE is now a SEQLOCK with
   copy-out API: type##_latest_copy(s, out) -> bool; *out unchanged on
   failure; mem_app renders a private copy (keeps last frame's on
   retry - never torn, never a dead pointer). All shared-buffer
   accesses are relaxed ATOMIC chunk copies (atomics never race under
   C11 - TSan-clean WITHOUT suppressions; plain movs on x86). GCC TSan
   rejects atomic_thread_fence -> AME_SNAP_FENCE shim (compiler barrier
   under GCC+TSan only; real fence everywhere else; Clang TSan keeps
   fences). tests/test_snap.c: 2 reader threads vs writer, 831k copies
   0 torn, TSan-clean; CI gained a tsan job (clang-19).
3. POOL LOST-DESPAWN (their repro: free(a),free(a),free(b) with queue
   cap 2 dropped b's despawn): slots_free now coalesces exact
   duplicates by (idx,gen) - the documented promise, finally true.
   overflow_drops only reachable beyond MAX_FREE DISTINCT frees per
   apply window; new coalesced counter; test_pool regression (the
   review's exact repro) + honest-overflow case. 32 checks.
4. TESTS WITHOUT EXAMPLES: -DAME_BUILD_EXAMPLES=OFF failed at
   $<TARGET_FILE:mem_server>. Standalone mem_server target built at
   top level when tests on + examples off. 17/17 in that config.
5. ASYNCINPUT GAME VARIANT did not build: (a) vendored lib needs
   _DEFAULT_SOURCE under our strict C23 (given to asyncinput_static);
   (b) variant target missed mem_net.c, stb + asyncinput include dirs,
   and src/input_asyncinput.c; (c) input_asyncinput.c itself had
   invalid C (atomic_fetch_add on _Atomic float; single-threaded
   accumulators are plain statics now).

BONUS RACE FOUND BY OUR OWN GAME-WIDE TSAN SWEEP (review missed it):
audio voice create/patch bypassed the SPSC ring - audio_new_synth/
audio_load_pcm/audio_set/audio_master wrote voice state directly from
the logic thread while the SDL callback mixed. All voice + master
mutation now travels as commands (NEW/SET_CFG/SET_PCM/MASTER; ame_aucmd
carries ame_synth_cfg + pcm ptr). Slot selection uses a logic-side
used_logic mirror + atomic env publication only. audio_voice_active
reads env (atomic), not the audio-owned playing flag. Audio is
inaudible in goldens -> screenshots byte-identical anyway.

VERIFY (this session, GCC 14): ctest 17/17 normal AND ASan/UBSan;
examples-off 17/17; asyncinput variant builds; TSan: test_snap +
test_pool + FULL autoplay-500 game = zero reports, screenshot
byte-identical to shots/stage2_particles.png; quiet frame-5 golden
byte-identical vs a c89b3db-built binary (cross-version proof).
NOTE: cmake/ninja/apt packages do NOT survive sandbox resets -
reinstall (see ci.yml apt list) when a new session cannot build.

NOTES: test count is now 17 (snap). ci.yml has a 5th job (tsan).
坑: python-generated C patches double macro backslashes; git stash +
parallel commands race (ASan configure once saw stashed sources).

SESSION 12 - STAGE 2 SHADOW MAPS (the named exit item after the
external-review fixes). One renderer, one shader, SECOND PASS of the
same uploaded batch - no second renderer (render.txt honored).

API (include/ame/render.h):
- rp_shadow(dir, center, extent): arm per frame (or once - state
  persists like rp_lighting). dir = direction the light TRAVELS;
  light ortho box side 2*extent centered at center; look_at fallback
  picks z-up for straight-down lights. rp_shadow_off() disarms.
- Casters = lit geometry (rp_set_lit stamps); unlit UI/text never
  casts (VS collapses a_lit<0.5 verts in the depth pass) and never
  receives (shadow term multiplied into the lit term only).
- 2048x2048 DEPTH_COMPONENT24 map, 3x3 PCF, slope-scaled bias
  (mix 0.0025..0.0005 by ndl). Shadow removes ONLY the diffuse direct
  term (subtractive); ambient + point light stay.

BIT-EXACTNESS (the standing contract): the FS line is
  c.rgb = c.rgb - v_diff * (1 - sh) * u_shadow_amt
with amt = 0 for non-shadow frames -> subtracts exactly 0.0 -> output
byte-identical to the one-pass path (test_render: outside-shadow
pixels byte-equal; existing goldens unchanged; quiet golden identical
to the c89b3db build). Lean (Ame/Shader.lean): shadow_term_off proves
amt=0 == unchanged; shadow_coord_test_rig / caster_nearer prove the
test rig's map coverage + occlusion order by kernel computation.
Mat4.ortho added to Ame/M4.lean (twin of the cglm-oracle-tested
ame_m4_ortho in math.h).

THE WAR STORY (two llvmpipe traps, both probed standalone in /tmp):
1. SAMPLER2DSHADOW POISONS FRAGMENTS on llvmpipe when the bound depth
   texture is invalid - AND we could not even get a uniform-gated
   branch to fire reliably on the engine program (branchless rewrite
   sidestepped it). FIX: plain sampler2D depth reads + manual
   comparison in the shader (portable everywhere), branchless amt
   multiply, shadow target created EAGERLY at rp_init (unit 1 always
   holds a complete depth texture).
2. THE DEEP ONE: shadow_target_ensure originally bound the depth
   texture on the ACTIVE UNIT (unit 0!) - the sprite-sheet unit. From
   that moment EVERY lit draw rendered black (unlit frames looked
   fine at first glance; a 483->483 test chase through scanlines,
   uniform readbacks, glGetError, a standalone EGL probe and 6
   bisect steps found it). RULE now in the code comment: shadow
   texture work happens on UNIT 1 ONLY; ensure() saves/restores FBO
   + active unit.

GAME: memory_game arms rp_shadow(ldir, table center, 5.0) at init.
Quiet golden (frame 5) BYTE-IDENTICAL (face-down flat cards hide
their own shadows). Autoplay-500 golden RE-BAKED deliberately
(shots/stage2_particles.png: standing card casts a thin dark band,
~279 px delta -120); shots/stage2_shadows.png added (frame 120).

VERIFY (GCC 14): ctest 17/17 normal + ASan/UBSan + examples-off;
asyncinput variant builds; TSan zero reports over full autoplay game
(screenshot byte-identical to the new golden); fresh default-config
build -Werror clean; lean lake build green.

REMAINING for Stage 2 exit: MATERIALS. Then opus decode (parity).
Lean note: pure core only (no mathlib): no linarith/norm_num in
proofs; closed numerics via native_decide; Rat lemma names verified
via #check probes.

SESSION 13 - OPUS DECODE + third_party->external RENAME + EXAMPLE
PORTS (user directive: "Add opus decode. Rename third_party to
external. Next is to rewrite each example, except yaml, from master
to here."). Commits this session: rename, opus+examples.

RENAME: third_party/ -> external/ (git mv; .gitmodules path updated;
7 files re-pathed). Fresh clone verifies: submodule at
external/asyncinput checks out from GitHub, builds 17/17.

OPUS (last A-Monogoose parity item, pulled ahead of Stage 3):
- Engine: audio_load_opus(path)/audio_load_opus_mem(bytes) in
  src/audio.c under AME_HAVE_OPUSFILE (pkg-config opusfile, found in
  top-level BEFORE any add_subdirectory - tools/tests gate on it).
  Same caller-owned stereo-float contract as audio_load_wav
  ("one C API for both", audio.txt). 48k native; linear deterministic
  resample when mixer rate differs. Stubs (NULL) when absent.
- tools/mkopus_sine.c: bakes the test asset at BUILD time - libopus
  ENCODER + HAND-MUXED minimal Ogg (OpusHead/OpusTags, 1 packet/page,
  Ogg CRC32 0x04c11db7 MSB-first; frames must be full 20 ms - pad the
  tail, opus_encode rejects 13 ms leftovers). Output
  tests/assets/baked_sine_opus.h (gitignored, generated).
- tests/test_audio_opus.c (18th test, gated opusfile+opus): duration
  window, zero-crossing frequency 420-460 Hz (codec-noise robust),
  rms level, L==R, decode-twice memcmp, corrupt-blob rejection,
  plays 2.02 s through the mixer. 18/18 total.

ENGINE SHOT HOOK: app_sdl.c now honors AME_SHOT_PPM=path +
AME_SHOT_FRAMES (default 5): zero-dep P6 PPM screenshot for ANY app
(engine stays stb-free; memory_game keeps its stb PNG AME_SCREENSHOT
path). All examples below smoke-verified headless with it.

EXAMPLE PORTS FROM MASTER (rewrite onto ame-next, no ECS/facades -
spec; each examples/<name>/ with own CMake, plain C app hooks):
DONE THIS SESSION:
- audio_pan: mouse-panned 440 Hz synth + level puck (master:
  flecs+Position component -> plain app_fixed).
- audio_opus: .opus player (AME_OPUS=path env), pan by mouse, level
  meter; decodes via the new engine API.
- audio_ray: concrete wall + listener + mouse source; live L/R gain
  bars driven by ame_audio_ray_compute (AME_MAT_CONCRETE).
- line_draw: freehand as thin rotated quads in the ONE batch (master
  used a second GL_LINE_STRIP pipeline; ours keeps one batch).
- tilemap_render: master's own sample.tmj through ame_tilemap_draw
  (stable flat tints), arrow-key scroll; asset copied to bindir.
COVERED ALREADY IN-TREE: memory_game (anchor game), dialogue (engine
module + tests = master's dialogue_example/dialogue_ui_example).
SKIPPED PER USER: scene_file (yaml; scene-YAML is spec-rejected).
REMAINING (next sessions, biggest-last): example_parent_child (90 l,
relative transforms by hand), prefab_smoke (70, data-driven spawn),
flecs_scene (248) + flecs_script_scene (ECS demos -> plain-C scene
setups), multithreaded_scripts (218, logic-thread demo), obj_loading
(assimp bake demo + .blend sample), physics_test (geometry queries),
raymarch_arcade (461, software raymarch into a sprite - one batch),
text_editor (671), rhythm_game (966), kenney_pixel-platformer
(1.9 M assets), unitylike_{minimal,audio_example,box2d_car,
pixel_platformer,platformer_ecs,scene_hierarchy_test} (C++ facade
demos -> plain-C ports of the same gameplay).

VERIFY: ctest 18/18; all 5 new examples build -Werror clean and shot
headless; memory_game quiet golden still byte-identical.

坑 this session: pkg finds must precede add_subdirectory(tools);
opus_encode needs standard frame durations; turn-boundary snapshots
may rewind .git (fetch remote + reset --soft recovers); git identity
vanishes each reset (set per-repo); AME_SCREENSHOT was game-level
(env handled in mem_app), engine uses AME_SHOT_PPM.

SESSION 14 - TWO MORE MASTER PORTS: raymarch_arcade + text_editor.
Commits: this one. Suite 18/18 unchanged; memory goldens unaffected.

ENGINE ADDITION: rp_update_texture(id, pixels, w, h, comps) - in-place
glTexSubImage2D for dynamic textures (dims/format must match; renderer
now tracks tex_w/tex_h/tex_comps). The software-rendered-content path
(CPU shades, uploads per frame, draws ONE quad through the batch).

raymarch_arcade (master: one fullscreen GLSL SDF shader game):
- ame-next keeps the ONE-batch doctrine: the same 2D SDF scene
  (player + falling orbs, smooth-union, glow, vignette, scanlines,
  state tint, hit flash, SDF-box HUD bars) is shaded ON THE CPU into
  480x270 RGBA8, rp_update_texture'd, drawn as one screen-anchored
  quad. Master's gameplay constants ported (45 s level, 0.15 health
  per hit, 0.6 s spawn rows of 1-3, sway formulas, 2 s compaction),
  scaled 1280->480; deterministic LCG (master used rand()).
- Threading per spec: gameplay in app_fixed (logic thread), state
  crosses via AME_SNAP_DEFINE seqlock; render keeps the last good
  copy on retry. TSan: ZERO reports in our sources (2 in libgallium
  driver internals at shutdown - third-party, documented).
- Verified headless: AME_SHOT_FRAMES 5 -> 90 lit-pixels 19511 ->
  27927 (enemies spawning + progress bar filling = gameplay alive).

text_editor (master: SDL_ttf texture + second solid-color shader):
- ONE batch: glyphs AND caret/selection rects are batch quads; the
  engine's baked atlas replaces SDL_ttf. Layout per line; per-glyph
  el[].x makes caret/selection geometry EXACT for any font.
- THREADING FIX worth remembering: text_layout is pure CPU, so BOTH
  sides keep their OWN ed_geom (logic re-layouts for mouse
  hit-testing, render for drawing) - sharing layout arrays across
  the fixed-step/render threads would race. State crosses via
  seqlock {rev, text, caret, sel_start, sel_end}; render re-layouts
  only on rev change. 32K buffer published per edit (memcpy trivial).
- Keys: raw scancodes + shift -> ASCII (engine input surface = the
  equivalent of master's asyncinput/xkb path); backspace/enter/
  arrows/home/end, shift+arrows extend selection, mouse press/drag
  selects (press sets anchor). ESC quits. Blink is presentation-only
  (SDL_GetTicks in render).
- 坑 found the hard way: text_draw_screen already adds
  rp_screen_origin INTERNALLY - do not pre-add ox/oy for text (the
  manual caret/selection sprites DO need it). Symptom: text flew
  offscreen at 2x offset, only caret visible.
- TSan: zero reports at all. Headless shot: 13414 lit px (6 lines).

PORT MATRIX UPDATE: done += raymarch_arcade, text_editor. Remaining:
example_parent_child, prefab_smoke, flecs_scene, flecs_script_scene,
multithreaded_scripts, obj_loading, physics_test, rhythm_game,
kenney_pixel-platformer, unitylike_{minimal,audio_example,box2d_car,
pixel_platformer,platformer_ecs,scene_hierarchy_test}.

SESSION 14b - USER BUILD FIX: libfyaml >= 1.0 under strict -std=c2x.
Their gcc hit "implicit declaration of posix_memalign" INSIDE
/usr/include/libfyaml/libfyaml-align.h (fy_align_alloc is static
inline there). Sandbox has libfyaml 0.8 (no such header) - could not
reproduce locally; fix is the standard idiom anyway. Added the
_DEFAULT_SOURCE request guard BEFORE all includes (in-source, we own
the files - CMake define was for vendored asyncinput only) in:
src/dialogue.c, tools/dlg2c.c, tests/test_dialogue.c. Fresh default
build -Werror clean, 18/18. 坑: sandbox libfyaml 0.8 vs user >= 1.0
- optional-dep code must compile under BOTH; strict-c2x POSIX hiding
 now fixed at THREE sites total (asyncinput, dialogue trio).

SESSION 15 - TEXT GRID FIX (user bug report): "Lean model of text
rendering is bad, if it allows errors... cursor has horizontal
offset... font is blurry. Master has similar problem that you
inherited."

ROOT CAUSE (one, both symptoms): advances are FLOAT (stb 17.375...),
pen accumulated fractionally, el.x stored fractional, alignment shift
added arbitrary float -> every glyph quad rasterized at its OWN
fractional origin (blur), and the editor caret/selection rounded
INDEPENDENTLY of the ink grid (horizontal offset; same class as
master's metrics-vs-renderer split).

FIX - "one snapped pen" model:
- src/text.c: pen_snap(x)=floorf(x+0.5f). Layout stores el.x/el.y
  SNAPPED; center/right shift snapped; out->w snapped. Draw
  (text_draw_screen) snaps its origin ONCE. Contract documented in
  include/ame/text.h: consumers read el[]/w DIRECTLY (caret_i =
  origin+el[i].x, EOL = origin+w) - never re-round. World path
  unaffected in principle (3D poses stay continuous).
- Editor: EOL fudge "+2.0f" REMOVED (violated EOL=w); manual
  caret/selection sprites now base on the SAME snapped origin
  (floorf(ox+PAD+0.5)-PAD pattern).
- tests/test_text.c: +4 grid-contract cases (117 checks total):
  integrality of el/w at scale 1 AND 1.15, no backsliding pens,
  align keeps grid.
- lean/Ame/Text.lean (pure core): snap = floor(x+1/2); pen i =
  sum(take i adv); caretX = snap pen; inkX = caret + xoff;
  caret_ink_exact (offset = bearing EXACTLY - omega), eol_caret_eq_
  width (rfl via function-indexed model - avoid list-index lemma
  fights: model pens as FUNCTIONS not lists), align_shift_preserves_
  gaps (omega); rig (advances 17, 8.25, 19.9, 9.1) with native_decide
  List.all bounds (NO forall-with-free-var in native_decide
  statements; no linarith/abs in pure core).

VERIFY: ctest 18/18; test_text 117 checks; lean lake build clean.
Goldens DELIBERATELY re-baked (both stage2_particles.png +
stage2_shadows.png): 4469 px changed = text regions ONLY, shadow
band x370-400/y545-665 had 0 (zero) changed pixels - numerically
isolated via PIL before re-baking.

USER DEFERRED LIST (fix later, their words): (1) memory game text is
not ON cards, it's just hidden and revealed - text should be printed
on the card faces; (2) no multiplayer; (3) no --help.

坑: cwd typos kill whole tool calls; test locals must not collide
(lc); native_decide statements must be CLOSED (List.all pattern).
